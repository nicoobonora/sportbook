/**
 * Layout minimale per la pagina di login unificata admin.
 * Nessun contesto club richiesto.
 */
export default function AdminLoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
